# 本地辅助工具实施与验证：2026-09-22

## 2026-09-22 PR #43 后续工具交付

[PR #43](https://github.com/MaginaLW/harness-model/pull/43) 已于 2026-09-22T11:08:56Z 普通合入 main
（`48bf777`）。本轮完成 runner 本机路径与 PSDrive 绑定检查，
以及显式证据选材预检；缺失日志、缺失原件和实质字节冲突均保留可见诊断。

固定源码 `2393c67`：本地 **2238 passed**，含 Python 工具总覆盖 **88.86%**、
累计差异覆盖 **99%**，七组质量检查通过。TASK-0052 正式 V1、独立实施审查、
代码批准和本地 Gate 均通过；精确发布 head `45a220f` 的 required CI
为 Linux **2236 passed / 2 Windows-only skips**、核心覆盖 **88.03%**。
维护模式远端 Verify and Gate 跳过，本地正式验证单独保留，不混用覆盖范围。

核对远端父提交、树与源码祖先后已关闭 TASK-0052：MERGED、Missing: none。
账本共 **51 项：42 MERGED / 8 BLOCKED / 1 APPROVED_FOR_MERGE**。详细版本与动作依据见
[TASK-0052 关闭记录](../../.ai/tasks/TASK-0052/publication-closeout-001.md)。
本轮关闭记录仅本地追加，不递归发布；三个用户草稿及旧历史保留。

E3 自然双产品案例、外仓安全交接、I1 其他生命周期及 E4/E5/I2–I5/阶段三四继续按
既有条件推进；TASK-0028 保持选项 C，七项更早 BLOCKED 不自动恢复；新增 TASK-0051 为已被本任务接续的 CI 失败历史。
本次首次 Linux CI 的 3 个失败来自合成 Windows 测试的宿主盘查询，保留为 TASK-0051
失败记录；`7b940bf` 的 6 行 fixture 修复保持所有原断言，Task52 重新完成验证后发布。
以下 PR #42 及更早段落是对应历史窗口，不作为当前待重复实施的队列。

### 本轮具体证据

- runner 独立安全反例确认：进程内本地 PSDrive 别名在修复前能读到合成 JSON，
  修复后在文件元数据读取前拒绝；普通本地对照仍成功，runner 与 discovery 的
  208 项兼容回归通过。没有共享访问或服务启停，路径检查不声称恶意并发隔离。
- `python -m tools.evidence.selection_review` 只核对显式映射，不修改或自动补选。
  TASK-0050 的 88 项选材通过，分别省略日志、原件均按预期失败；TASK-0049 的
  43 项日志引用在本轮新时间只读复核通过，省略 stdout 后失败。旧原件摘要未变；
  此新记录不冒称历史原始回执、旧任务重跑或完整 producer 配对重审。
- 初版质量运行因独立审查发现 PSDrive 问题中止，失败与修复前后材料保留；最终
  完整源码质量来自 `full-quality-002`。正式 V1 单元 1600、回归和覆盖率轮各
  2238 passed、核心覆盖 88.1167%。Python 数值覆盖不代表 PowerShell 行覆盖。

## 2026-09-22 PR #42 合并与本轮关闭

[PR #42](https://github.com/MaginaLW/harness-model/pull/42) 已于 08:06:49 UTC 合入 main
（`ae0e3d3`）。最终 head `1d76edd` 的 required CI 通过：Linux 2102 passed、
1 Windows-only skip、核心覆盖率 88.03%；本地含工具测试 2103 passed、总覆盖
88.58%、累计差异覆盖 96%。TASK-0050 正式 V1、独立审查和本地 Gate 均通过，
核对远端父提交、树及源码祖先后已关闭 MERGED、Missing: none。

账本现为 49 项：41 MERGED / 7 BLOCKED / 1 APPROVED_FOR_MERGE。工具与本轮 E1
诊断已发布；条件阶段、外仓交接和 TASK-0028 选项 C 保留。动作及关闭回执仅追加
本地，不递归再发布；详见 [TASK-0050 关闭记录](../../.ai/tasks/TASK-0050/publication-closeout-001.md)。
下文关于最终发布尚待执行的文字属于此前观察窗口，原文保留。


本轮按所有者持续推进待办的授权，完成证据移交工具、受控环境工具发现，以及登记试点
当前证据诊断。工具属于维护模式下的独立 task-free 范围，没有修改内核、Policy、Schema、
CI、忽略规则或既有任务账本。原质量阈值和分支保护保持不变。

## 最终源码补充核验与发布流程纠正

最终实现 `5ecde7164af91f166c13ce1953e3878961a45bff` 补充拒绝从树外输入向任意 Git
仓库写入归档；判断 Git 标记使用不跟随链接的元数据读取，增加三个针对性用例。
原 `7702b7d` 的检查保留为历史结果，最新独立干净检出重新完成全部七组检查：
**2103 passed**，606.11 秒，含两个工具的总覆盖率 **88.58%**；可执行差异 467 行、
缺失 15 行，diff coverage **96%**。Ruff、format、内核与工具 mypy、whitespace
均通过，最终检出无跟踪差异。证据工具 93 项、诊断工具 52 项测试通过；43 文件
移交包用最终源码再次读回通过，原归档摘要不变。

工具实施依维护模式 task-free 完成；后续外部发布属于 AGENTS.md 的 AI Flow 升级
清单。首次 PR #42 推送和创建，以及先前 PR #41 收尾发布，虽有当前所有者明确授权
和树外动作记录，但没有建立独立发布 task，存在流程遗漏。原先时间和记录保留；
不把后建任务倒签为先前批准，不重开已关闭任务。

[TASK-0050](../../.ai/tasks/TASK-0050/spec.md) 仅前瞻覆盖接下来的 PR #42 推送、
更新和合并及相应治理记录，路线由 CLI 核定 REVIEW / V1。它分别检查创建基线到
受检 subject 的任务范围、远端 `aeaed58` 到最终 head 的完整交付范围。旧 PR #42
head `20a948c` 的 CI 通过仅覆盖旧候选；最终发布还需其精确 head 的新 required CI、
现行保护检查和实际合并核验。当前段落不预告这些后续步骤已经通过。

以下实施与验证段落保留初次固定版本的观察结果；最新源码结果以上段为准。

## 实现与范围

| 待办 | 本轮交付 | 实际限制 |
| --- | --- | --- |
| 证据可迁移性 | [显式选择、原始字节导出和只读校验](evidence-handoff.md) | 不认证来源、不恢复环境、不解释证据内容或产生 Gate；Git 文本与运行原件须由交付方明确选择 |
| I1 受控依赖发现 | [固定工具定位与严格版本诊断](controlled-tool-discovery.md) | 真实采集为 Windows；操作者指定本地可信目录，不是 OS 身份或安装隔离证明；不安装、注册或启停服务 |
| E1 当前窗口 | [登记试点的版本化诊断](pilot-evidence-review-2026-09-22.md) | 外仓未修改、未重新验收；实际回灌仍由目标安全交接边界处理 |

实施阶段启用 2 名 sub-agent，分别独占证据工具及诊断工具文件；主 agent 处理实际发布、
材料选择与集成。随后两名 sub-agent 交叉只读审查，作者修复各自文件，主 agent 统一
提交。固定源码后的干净检出验证、发布身份核对及合并按依赖串行。

固定源码 subject 为 `7702b7da67ad8eda3da2dc999ca50f18f01a8902`，质量比较基线为
`aeaed58272f90f1505fce3841d302f5be6317119`（PR #41 实际合并）。
实现提交为 `dba7f56`、`22fe17e`、`7702b7d`；E1 文档另在 `524498f` 提交。
后续文档不改变这次受检实现，不将文档提交冒充源码重新验证。

## 已完成验证

- 干净独立检出完整测试 **2100 passed**，616.16 秒；覆盖率轮同时包含 `aiflow` 和两个
  新 Python 工具，总行及分支覆盖率 **88.57%**，85% 门槛通过。
- 两个新增工具可执行差异共 460 行，缺失 15 行，diff-cover 显示 **96%**，90% 门槛通过。
  工具各自含分支覆盖率为 evidence **94%**、diagnostics **99%**。PowerShell 桥接不在
  Python 覆盖率统计中，其解析及真实 Windows 调用另有实际验证。
- Ruff、format、内核 mypy 和两个工具的 strict mypy、whitespace 均通过；检出没有
  跟踪文件差异。全部七组检查退出 0，完整日志与 coverage XML 保存在树外材料目录。
- 定向回归为 evidence **90 项**、diagnostics **52 项**；独立交叉审查和主 agent
  集成审查发现的问题全部修复，无未解决发现。这是同产品 sub-agent 审查，不是 E3。
- 额外 208 个有界小样本得到 35 次成功、173 次结构化拒绝，零未捕获异常；成功样本
  包括原包及不改变字节契约的元数据变化，不将所有变异都误称应拒绝。

审查修复覆盖源文件原子替换、累计写入预算、ZIP 中央目录分配前限额、网络/设备路径、
祖先 junction 访问顺序、输出 ADS/保留设备名，以及短读、非法编码和超长整数的错误出口。
输入文件系统仍需保持静止；没有把路径检查或哈希校验声称为恶意并发隔离或可信执行。

## 真实使用检查

在受检提交的独立检出中，Windows 空 PATH 下执行完整、缺 PowerShell、版本不匹配
三种真实诊断，退出码分别为 0、1、1；UNC 输入在文件元数据读取前退出 2。观察到的
Python/Git/PowerShell 版本分别为 3.11.9 / 2.55.0.windows.3 / 7.6.6，仅是该窗口主机
事实，不是推荐版本。报告保持 `runner_ready=false`、`gate_pass=false`。

TASK-0049 的真实材料进行了导出、可信摘要读回、内容篡改、整包篡改和禁止覆盖检查。
最终私有移交包为 **43 份材料、519,503 字节**，整包 SHA256：
`d8df2e8e5f3919acc9bcce8b820cf4dde0ffd23366754cae668e745e33c05fae`。
10 份文本逐一核对为固定提交的 Git LF blob，另保留两份不同字节的 CRLF 工作原件；
原 V1 evidence 与全部 20 份引用 stdout/stderr 保持原始摘要。另一名审查者完成独立读回。

交付包包含原件、摘要、Git 对照、历史工具环境声明及重放指引。源码、锁文件和依赖环境
仍须从固定仓库提交另行取得；本轮没有重建历史环境、重新签发批准或重跑旧任务验收。
原件和运行路径仅留树外；本仓只记录不含机器路径的结论和摘要。

## 后续边界

当前两项具体工具交付完成，不将其扩展成 I1 全部生命周期完成。E3 仍需自然双产品
案例及可核实会话来源；E4/E5/I2–I5、阶段三四继续按原条件推进。TASK-0028 选项 C
和七项历史 BLOCKED 均保持，不因本轮工具或诊断重复关闭任务、生成批准或制造业务修改。

本记录是固定版本的本地验证事实；发布候选另经 required CI 和真实远端合并核验。
不把这些本地检查写成远端已通过，也不为登记发布回执递归制造新任务。
