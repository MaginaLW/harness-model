# ZCode 试点复盘与接续：2026-09-20

本次是固定观察窗口，从本仓 `d40971ae7a330431e9e44ea1c86d30e95ae07905` 出发。
两名 sub-agent 分别只读核查已登记试点，主会话读取本仓 CLI、实施记录及关键源文件；
本报告不替代目标原有权威状态，不补造历史模型会话、CI 或实验验收。

## 当前状态

| 对象 | 本轮核实 | 限制与接续 |
| --- | --- | --- |
| harness-model | 分支 `codex/self-hosted-runner-inventory`；起点相对本地 main 领先 96、落后 0；仅三份既有未跟踪计划稿 | 未查询实时远端，领先计数不表示待发布授权 |
| 运行账本 | 47 项：38 MERGED、7 BLOCKED、1 APPROVED_FOR_MERGE、1 IMPLEMENTING | TASK-0048 Missing 为 implementation_result；TASK-0028 Missing 为 reverification，继续保留选项 C |
| 任务 02 | 最新跟踪记录已到阶段 57：WHPX 启动与首次严格 SSH 成功 | 本轮未回读树外原件或 guest；阶段 55 完整 POSIX 退出 124 不变，新 boot 后完整门未完成 |
| E1 / E2 | 旧交付的证据诊断和最小 guided 设计已有产物，本轮补充新观察 | 诊断收口不等于目标当前 HEAD 验收；设计不等于 E3 执行 |
| E3 / E4 / E5 | 尚无足以核定完整双产品交接的新增证据；不据此启动内核桥接或条件性扩展 | 本轮两个 sub-agent 是同一产品内分工，不能算双产品实验 |
| 阶段三 / 四 | 仍 not_started | 目标项目自己的 Phase 3/4 与本仓阶段编号没有验收对应关系 |

CLI 重放入口：`python tools/analysis/approval_overhead.py --format text`、
`python -m aiflow status TASK-0048`、`python -m aiflow status TASK-0028`。
TASK-0048 的 classification/approvals 仍显示 fresh/current，缺实施结果；不据此新增人审，
也不将规格批准当作具体外部动作授权。

## dotfiles：反馈已应用，执行入口仍会漂移

固定检出 `eeedc4611990284d186159734c18f1cf92646cd8`，本地 main 与 origin/main
一致，工作树干净。以下路径均相对该项目：

- `docs/ZCODE.md:498-526` 已记录第十三次反馈及 upstream `d40971a`，包含声明预算与
  实际平台上限的区别、对独立评审结论本身的核查，以及上游完整验证记载。旧 `68f3dcb`
  接入快照已不是最新窗口；这些是方法应用证据，不是效率或缺陷率改善的因果证据。
- `scripts/live-safety-policy.psd1:4` 实际为 `released`，而 `AGENTS.md`、`STATUS.md`
  首页和 `docs/ZCODE.md:32` 仍称 `interlocked`。`status/active/live-safety-hardening.md:3410-3455`
  又将其描述为 local candidate、实验未做、Steps 2–5 待所有者。接手时必须分开看可执行
  策略、文档声明和实验验收，不能只凭首页判断安全状态。
- 本地 origin/main reflog 有 `eeedc46` 的 `update by push`（09-19 22:25 +08:00）。
  这是本地推送记录；本轮未查询实时远端，也未核查该次授权。它与“实验前不得推送”的
  当前候选文字存在待核对关系，不能反向推断实验已经通过或认定当时未经授权。
- `tmp/unified-phase4-window.json` 原件可读，SHA-256 与 active 的 `3388-3389` 记载匹配，
  摘要为 42/42、零失败/超时/重复/缺失/tree-kill。被测版本 `d30360f` 来自配套记录，
  哈希匹配本身不证明版本绑定；尤其不能覆盖之后 `bffa7d7` 的 released 变更。
- active 的 `3218-3227` 已更正 task-skills 归因为 22/0、错误发生于 hard-kill load hash；
  `3394-3400` 已记载同 SHA 复跑结论。但 `docs/CI_FAILURE_RULES.md:266-269,289,292` 仍保留
  旧失败或待复跑表述。最新 active 更正不等于仍供执行的规则正文已同步。
- `tmp/ci-128.json` 是历史 in_progress 缓存；`tmp/lab8/evidence` 在本轮列举为空。
  二者不能证明当前 CI 结果或所有位置都没有实验原件，当前实验验收仍为未核实。

处置：本仓补充“配置、发布证据、业务验收及规则正文分别核对”的接手方法。
目标入口修正、实验授权/原件及最新 CI 核查交还原任务持有者；未修改目标文件或策略。

## r3s：持续产生回执，当前版本与历史通过仍须分开

固定检出 `9e2d8014c60eb760c159e931b6f9af5847883782`，main、工作树干净。
以下证据来自该提交的 `docs/PROJECT_STATUS.md`；本轮未调用远端 API、访问生产或
回读其树外 Strict/CI 原件，因此运行结果均标为**仅项目记载**：

- `218-223`：第 34 轮 Windows 成功绑定 `4bc3b3e`；POSIX 零步骤被拒，整次 run 为
  failure。最新 HEAD 对应第 35 轮仅登记触发，未读到结果，不能宣称当前双 lane PASS。
- `219,223`：c28 Strict 17/17；后续 schema-v8 批次编号更正为 c29，与合并批共用一次
  捕获。合并字节、回执编号及实际被测版本应一一核对，不能把各批都算独立验证样本。
- `574-578,493`：记载应用 upstream `3962151`，后续 `bda5e82` 评估为已有等价方法。
  “评估无新增适配”与“应用最新版”分开。`509,577` 已在实际报告中分层记录证据、预算
  和非确定输出，可以确认方法有落地记载，效果大小仍 unknown。
- `581-588`：09-09 指定范围的独立终审三次未完成。没有发现针对该范围补齐终审的
  新记录；其他批次评审不能代替，也不把本轮只读复盘计作 E3 闭环。
- `220,403`：控制字节/转义腐蚀反馈仍因来源非净及并发迹象挂起，第 14 次复查仍未应用。
  保留为检查候选；未来若实现，应明确文件类型、合法 CRLF 与非法控制字节，不将全部 CR
  判为错误。本轮不为一条反馈新增全仓扫描器。

并发批次共享 manifest、回执与状态入口的纠缠再次说明需要明确文件所有权和串行整合。
这已被本仓并发约定及接入方法覆盖，本轮不重复添加规则。三份未跟踪原稿始终保留；
它们的存在本身不能证明活跃 writer，反过来工作树干净也不能证明写入权已经交还。

## 本轮推进和下一步

1. **本轮完成范围：** 读取当前状态、复盘两个真实窗口、更新维护入口，并补充一个有
   具体反例支持的方法。既有历史记录保留；不重跑外仓业务来补过去的证据。
2. **串行恢复任务 02：** 先从阶段 57 的树外 `tools/stage57-handoff/NEXT-STEP.md`
   核对实际身份和材料，再验证新 boot 的低权限、sudo 禁止及挂载合同。不能重放旧
   guard 或关机脚本。原 current-state fixture 必须保留 600 秒、kill-after 30 秒；
   通过后才执行未缩减完整 POSIX。本轮未执行这些运行时步骤，具体外部动作核当前权限。
3. **可并行准备：** 恢复时计划 1 名 sub-agent 只读核固定 source/runtime/image/policy
   摘要，主会话处理 readiness；fixture、完整门、运行回执和账本推进严格串行。
   r3s 接单窗口仍需原持有者交接；注册、服务切换、CI、push、采用不能从本报告推导。
4. **E3 接续：** 在自然发生的真实任务中，保存固定受审输入、问题来源、修复前后版本、
   验证原件和逐项独立复核的关联。缺少的双产品会话不补造；据结果再判断 E4 是否必要。
5. **回灌：** 两仓的写入权均未在本轮明确交接，因此本轮只报告 pending；本仓方法通过
   原质量门并形成真实提交后，目标持有者可按原闭环逐条评估，无差异则 no-op。

本轮文档交付与 TASK-0048 的治理实现分别处理：不改账本、Policy、Schema、CI 或运行时。
本仓质量验证只覆盖本轮候选，不替代目标验收；具体结果保存在树外本轮质量回执，
通过前不将候选记为已验证上游，不推送或合并。
