# ASK pilot summary

- Decision: generate Markdown only (`OPT-01`).
- Actor: user.
- Governed route: ASK.
- Verification: V1 is required before Gate.
- Scope: this summary file only; no JSON counterpart is generated.
- External actions: none.

The choice is preserved in the task's `ask_answered` event, `decisions.md`, and frozen
specification. Missing answers were shown to block both implementation and Gate.
