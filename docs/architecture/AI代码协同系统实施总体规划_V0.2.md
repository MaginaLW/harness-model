# AI 代码协同系统实施总体规划

> 版本：V0.2  
> 文档定位：在《AI 代码协同分流与模型路由设计 V0.1》的概念框架基础上，形成可落地的实施总纲。  
> 当前目标：先建立可运行的最小闭环，再逐步加入模型信任度、自动路由和更高级验证能力。

---

## 目录

- [1. 建设目标](#1-建设目标)
- [2. 总体架构](#2-总体架构)
- [3. 两个独立的控制维度](#3-两个独立的控制维度)
- [4. 标准任务流程](#4-标准任务流程)
- [5. 系统组成与职责](#5-系统组成与职责)
- [6. 建议目录结构](#6-建议目录结构)
- [7. 关键数据与状态管理](#7-关键数据与状态管理)
- [8. 验证体系](#8-验证体系)
- [9. 模型选择与信任度](#9-模型选择与信任度)
- [10. 最小可行版本](#10-最小可行版本)
- [11. 分阶段实施计划](#11-分阶段实施计划)
- [12. 暂缓实现的内容](#12-暂缓实现的内容)
- [13. 验收标准](#13-验收标准)
- [14. 核心原则](#14-核心原则)

---

## 1. 建设目标

本系统用于规范人类、Claude Code、Codex 及其他模型共同完成代码任务的过程，重点解决四类问题：

1. 哪些决策可以由 AI 自动完成，哪些需要用户选择或人工批准；
2. 不同任务应使用多强的验证体系；
3. 不同角色应选择哪个模型，以及为何值得支付相应费用；
4. 如何通过可执行规范、自动测试、质量门和可复现证据提高最终成功率。

系统不以增加审批次数为目标，而以以下结果为目标：

- 人工注意力集中于方向、风险和权限判断；
- 低风险任务可以高效自动执行；
- 高风险任务在执行前后均有明确控制点；
- 所有关键结论均有与具体代码版本绑定的证据；
- 任务失败后能够定位、升级和恢复。

---

## 2. 总体架构

系统采用“规则说明、流程编排、确定性执行、运行拦截、最终强制”五层结构：

```text
AGENTS.md / CLAUDE.md
        ↓
Skills 与角色 Agent
        ↓
规则引擎与任务状态机
        ↓
Hooks 与本地验证程序
        ↓
CI、分支保护与人工批准
```

各层定位如下：

| 层级 | 主要职责 |
|---|---|
| Agent 文件 | 声明所有任务必须遵守的常驻原则和入口 |
| Skills | 编排分流、ASK、REVIEW、验证和证据生成流程 |
| 规则引擎 | 确定分流、验证等级、权限和允许的状态转换 |
| Hooks | 在编辑、命令执行、提交等节点拦截越权动作 |
| CI 与分支保护 | 对最终提交实施不可绕过的质量和审批要求 |

实现原则是：

> 由模型负责理解、解释和生成候选方案；由程序负责规则计算、状态校验和强制门禁。

---

## 3. 两个独立的控制维度

### 3.1 分流模式：决定谁拥有决策权

系统以决策单元为基本对象，分为：

- `AUTO`：AI 在规定范围内自主执行；
- `ASK`：AI 提供 2—4 个实质不同的选项，由用户决定方向；
- `REVIEW`：AI 完成准备和实现，关键节点由人或独立审核者批准；
- `BLOCK`：当前缺少安全执行所需的信息、权限或保护条件；
- `ESCALATE`：执行中发现新风险，动态提高分流等级。

分流等级可以自动上调，但执行 Agent 不得自行下调。

### 3.2 验证等级：决定需要多强的证据

分流模式与验证强度不得简单绑定。建议设置：

| 等级 | 适用范围 | 主要验证内容 |
|---|---|---|
| `V0` | 极低风险机械修改 | 语法、格式、Lint、范围检查、Smoke Test |
| `V1` | 常规局部代码任务 | V0、单元测试、回归测试、类型检查、变更覆盖率 |
| `V2` | 跨模块或较易出错任务 | V1、可执行规范、验收测试、集成测试、定向变异测试、独立验证 |
| `V3` | 高风险和核心任务 | V2、安全检查、故障注入、Dry-run、回滚演练、人工规格与放行批准 |

任务最终配置应同时包含两个字段：

```yaml
route: REVIEW
verification_level: V3
```

例如，复杂但高度可验证的迁移任务可以是 `AUTO + V3`；简单但涉及生产数据的操作可以是 `REVIEW + V2`。

---

## 4. 标准任务流程

```text
任务输入
  ↓
创建任务记录并拆解决策单元
  ↓
应用硬规则与初步风险评估
  ↓
确定 AUTO / ASK / REVIEW / BLOCK
  ↓
确定 V0 / V1 / V2 / V3
  ↓
选择规划、实现和验证模型
  ↓
处理 ASK 或规格审核
  ↓
冻结可执行规范
  ↓
RED → GREEN → REFACTOR
  ↓
运行对应验证擂台
  ↓
生成 evidence 与 review package
  ↓
根据验证结果重新评估剩余风险
  ↓
自动放行 / 人工批准 / 动态升级 / 阻断
```

系统包含两次关键判断：

1. **执行前分流**：决定谁可以开始做、需要何种验证和权限；
2. **验证后放行**：根据真实证据判断是否允许合并、执行或发布。

---

## 5. 系统组成与职责

### 5.1 `AGENTS.md` 与 `CLAUDE.md`

只保存简短且稳定的常驻规则：

- 所有代码任务必须进入 AI Flow；
- 禁止绕过任务状态和验证门；
- Agent 不得自行降低分流或验证等级；
- 高风险动作需要单独批准；
- 所有证据必须绑定当前 commit；
- 完整规则与模板的位置。

不在其中放置完整评分公式和长篇工作流，以减少上下文占用和重复维护。

### 5.2 Skills

第一版建议建立三个 Skill：

#### `ai-flow`

负责：

- 创建任务；
- 拆解决策单元；
- 调用规则引擎；
- 处理 AUTO、ASK、REVIEW、BLOCK；
- 推动任务状态转换。

#### `ai-review`

负责生成结构化审核包：

- 审核目标；
- 功能背景；
- 代码地图；
- 语义变更摘要；
- 风险和未验证项；
- 验证证据；
- 需要人工回答的问题；
- 推荐结论。

#### `ai-verify`

负责：

- 读取验证等级；
- 调用统一验证程序；
- 收集测试、覆盖率、变异测试和质量门结果；
- 生成与 commit 绑定的 evidence。

### 5.3 角色 Agent

建议定义四类角色：

| 角色 | 职责 | 主要限制 |
|---|---|---|
| Planner | 拆解任务、识别方向问题、生成规格 | 不得批准自己的规格 |
| Implementer | 测试先行、实现和重构 | 不得修改规格以迁就实现 |
| Verifier | 独立设计反例、验收和变异测试 | 不沿用实现者完整上下文 |
| Reviewer | 将证据压缩成人类可审核材料 | 不替代最终风险接受者 |

V0、V1 任务可以合并角色；V2、V3 应提高上下文和模型独立性。

### 5.4 规则引擎与状态机

建议使用 Python 实现统一 CLI，例如：

```text
aiflow start
aiflow classify
aiflow answer
aiflow approve
aiflow verify
aiflow status
```

核心职责：

- 读取任务属性和政策配置；
- 应用不可被平均掉的硬规则；
- 输出分流模式和验证等级；
- 选择候选模型组合；
- 检查任务状态转换是否合法；
- 记录升级原因和批准记录；
- 防止证据与代码版本失配。

### 5.5 Hooks

Hooks 用于运行时拦截：

- 执行删除、覆盖、推送和安装依赖前检查权限；
- 修改文件后检查是否超出允许范围；
- 修改规模扩大时触发升级；
- 提交前强制运行必要检查；
- Agent 结束前检查 evidence 是否完整。

高风险限制应由脚本退出码或系统权限真正阻断，而不是仅依赖提示词。

### 5.6 CI 与分支保护

CI 是最终不可协商的裁判，至少检查：

- 当前任务记录是否合法；
- 所需验证是否全部完成；
- evidence 是否绑定 PR 最新 commit；
- 必要的人类批准是否存在；
- 分支保护是否禁止直接推送和强制推送。

---

## 6. 建议目录结构

```text
project/
├─ AGENTS.md
├─ CLAUDE.md
│
├─ .ai/
│  ├─ policy/
│  │  ├─ routing.yaml
│  │  ├─ hard-rules.yaml
│  │  ├─ verification-levels.yaml
│  │  ├─ permissions.yaml
│  │  └─ model-registry.yaml
│  ├─ roles/
│  │  ├─ planner.md
│  │  ├─ implementer.md
│  │  ├─ verifier.md
│  │  └─ reviewer.md
│  ├─ templates/
│  │  ├─ task.yaml
│  │  ├─ spec.md
│  │  ├─ ask.md
│  │  ├─ review-package.md
│  │  └─ evidence.json
│  └─ tasks/
│     └─ TASK-001/
│        ├─ task.yaml
│        ├─ classification.json
│        ├─ decisions.md
│        ├─ spec.md
│        ├─ review-package.md
│        └─ evidence.json
│
├─ .claude/skills/
│  ├─ ai-flow/SKILL.md
│  ├─ ai-review/SKILL.md
│  └─ ai-verify/SKILL.md
│
├─ tools/
│  ├─ aiflow.py
│  ├─ router.py
│  ├─ task_state.py
│  ├─ model_selector.py
│  ├─ gauntlet.py
│  ├─ evidence.py
│  └─ hooks/
│
└─ .github/workflows/
   └─ ai-quality-gate.yml
```

在你的 dotfiles 仓库中，可维护通用 Skill、模板和基础脚本；具体项目只保留项目级政策、测试命令和任务记录。

---

## 7. 关键数据与状态管理

### 7.1 任务记录

`task.yaml` 至少包含：

```yaml
task_id: TASK-001
objective: 修复文件同步冲突
route: REVIEW
verification_level: V2
allowed_scope:
  - scripts/**
  - tests/**
forbidden_actions:
  - git_push
  - delete_without_backup
required_approvals:
  spec: human
  execution: human
```

### 7.2 状态机

建议使用以下主状态：

```text
NEW
→ CLASSIFIED
→ WAITING_FOR_ASK / WAITING_FOR_SPEC_REVIEW / READY_TO_IMPLEMENT
→ IMPLEMENTING
→ VERIFYING
→ VERIFIED / FAILED / ESCALATED
→ WAITING_FOR_FINAL_REVIEW
→ APPROVED_FOR_MERGE
→ MERGED
```

`BLOCKED` 可在多个阶段进入。所有状态转换均由规则引擎校验，不应依赖 Agent 直接修改字段。

### 7.3 版本绑定

审核和证据必须记录：

- 仓库与分支；
- 基础 commit；
- 当前 commit；
- 规格版本；
- 验证命令；
- 工具版本；
- 生成时间；
- 未验证项。

行为性代码发生变化后，旧证据和旧批准自动失效。

---

## 8. 验证体系

验证体系采用 Bob 思路，但不通过重复相似审核来提高可靠性，而通过多种独立证据覆盖不同故障模式。

### 8.1 可执行规范

规格应同时包含：

- 人类可读的目标、边界、错误行为和兼容要求；
- 可由测试执行的验收条件；
- 对高风险副作用和权限的明确禁止项。

ASK 的结果必须写入并冻结规格；REVIEW 任务的关键规格应先由人批准。

### 8.2 测试与质量门

| 机制 | 主要作用 |
|---|---|
| 单元测试 | 检查函数和局部算法 |
| Gherkin/验收测试 | 检查用户行为和跨模块流程 |
| 回归测试 | 防止破坏旧行为 |
| Property-based Test | 检查大量输入和不变量 |
| 覆盖率 | 定位未执行的新代码和关键分支 |
| 变异测试 | 判断测试能否识别典型错误 |
| 静态检查 | 检查类型、结构、依赖和安全问题 |
| QA/Dry-run | 检查真实环境、权限、性能和故障场景 |

覆盖率应优先考察变更行、分支和关键路径，不以全仓库总覆盖率作为唯一指标。变异测试优先针对新增代码和关键规则，不必第一版全仓库运行。

### 8.3 工程计算任务扩展

MATLAB、数值算法和工程仿真任务还应加入：

- 解析解或已验证基准解；
- 物理不变量和量纲检查；
- 网格与时间步收敛性；
- 对称性、守恒性和零输入检查；
- 固定随机种子和统计置信区间；
- 与商业软件、旧程序或文献算例交叉验证。

### 8.4 Evidence

最终 evidence 不只声明“测试通过”，还应包含：

- 当前任务和代码版本；
- 各项检查的原始结果摘要；
- 覆盖率与变异测试结果；
- Dry-run 和回滚结果；
- 未验证场景；
- 一条可重复执行的统一命令。

统一入口建议为：

```text
python tools/gauntlet.py --task TASK-001
```

---

## 9. 模型选择与信任度

### 9.1 选择顺序

先决定治理分流和验证等级，再决定模型组合，避免循环判断。

### 9.2 模型配置

信任度应按以下条件定义：

```text
模型 × 任务类型 × 角色 × 推理档位 × 工具环境 × 上下文质量
```

至少记录：

- `T_plan`：规划信任度；
- `T_impl`：实现信任度；
- `T_ask`：选项生成信任度；
- `T_review`：审核信任度；
- `T_tool`：工具使用信任度；
- `T_calibrate`：不确定性校准能力。

### 9.3 不同模式下的策略

- `AUTO`：在满足可靠性和权限阈值的模型中选择预期总成本最低的配置，而非一律使用最便宜模型；
- `ASK`：优先保证选项完整、互斥、可解释，必要时用强模型生成选项，再由较便宜模型执行；
- `REVIEW`：实现模型按任务专长选择，审核模型优先选择高性能且与实现者独立的配置；
- `V3`：必要时使用不同模型和独立上下文进行交叉验证。

### 9.4 数据来源

第一版可人工维护模型能力表：

- 外部评测作为先验；
- 内部任务成功率逐步提高权重；
- 记录返工次数、审核缺陷、费用、时间和越权情况；
- 执行异常可降低当前任务的动态信任度并触发升级。

---

## 10. 最小可行版本

第一版只实现以下五个核心组件：

1. 简短的 `AGENTS.md` / `CLAUDE.md` 入口规则；
2. 一个 `ai-flow` Skill；
3. 一组 `policy.yaml` 配置；
4. 一个负责分流和状态管理的 `aiflow.py`；
5. 一个统一验证入口 `gauntlet.py` 和基础 GitHub Action。

最小闭环：

```text
用户提出任务
→ ai-flow 创建 task.yaml
→ aiflow.py 输出 route 与 verification_level
→ 必要时完成 ASK 或规格批准
→ AI 实现
→ gauntlet.py 运行验证
→ 生成 evidence.json
→ CI 判断是否允许合并
```

第一版采用规则表和人工维护信任度，不开发复杂公式和多模型自动调度平台。

---

## 11. 分阶段实施计划

### 阶段一：流程可运行

目标：建立最小闭环。

实施内容：

- 任务模板和状态机；
- AUTO、ASK、REVIEW、BLOCK 基础硬规则；
- `ai-flow` Skill；
- `aiflow.py` 基础命令；
- V0、V1 验证；
- evidence 版本绑定；
- 基础 CI 门禁。

### 阶段二：审核和强化验证

目标：使 REVIEW 与 Bob 式验证可实际运行。

实施内容：

- `ai-review` 与 `ai-verify` Skill；
- 结构化审核包；
- V2 验证；
- Gherkin、集成测试和定向变异测试；
- 独立 Verifier；
- 动态升级规则；
- Hooks 范围检查和高风险命令拦截。

### 阶段三：高可靠性与模型路由

目标：支持重要工程项目和自动成本优化。

实施内容：

- V3、Dry-run、回滚演练和安全检查；
- 模型能力注册表；
- 内部成功率和费用记录；
- 自动模型选择；
- Claude Code 与 Codex 角色分配；
- 多模型交叉验证；
- 更严格的 GitHub 分支保护。

### 阶段四：独立编排器

仅在跨平台、跨模型任务量明显增加后实施：

- 统一模型适配器；
- 并行与暂停恢复；
- 审批界面；
- 费用预算；
- 多机器状态同步；
- 自动更新模型信任度。

---

## 12. 暂缓实现的内容

以下问题保留接口，但不进入第一版：

- 完整的风险评分公式和最优权重；
- 实时抓取各排行榜并自动更新模型评分；
- 精确的概率化信任度校准；
- 全仓库变异测试；
- 完整 Web 审批界面；
- 供应商无关的自动多模型调度；
- 自动合并所有 AUTO 任务；
- 复杂组织权限和多人审批。

原因是这些内容会显著增加复杂度，但不是验证核心流程是否有效的必要条件。

---

## 13. 验收标准

第一版完成后，至少应满足：

1. 每个代码任务都有唯一任务记录；
2. 系统能够输出分流模式和验证等级；
3. ASK 能生成 2—4 个具有实质差异的选项并保存决定；
4. REVIEW 能生成不依赖完整历史对话的审核包；
5. AUTO 任务受到文件范围、命令权限和验证门约束；
6. 代码发生行为性变化后旧证据自动失效；
7. 统一命令能够重新运行全部要求的验证；
8. CI 能阻止缺少证据或批准的任务合并；
9. Agent 在发现范围扩大或验证异常后能够升级任务；
10. 至少能在一个实际仓库中完整执行一次 AUTO、ASK 和 REVIEW 流程。

---

## 14. 核心原则

1. 分流对象是决策单元，不是整个任务。
2. 分流决定决策权，验证等级决定证据强度。
3. AUTO 是受约束的自主执行，不是无条件放权。
4. ASK 负责压缩决策空间，并将结果冻结进规格。
5. REVIEW 依赖结构化审核包，不要求人重新读取全部上下文。
6. 规格、测试、实现和审核应尽量保持角色独立。
7. 模型主要降低错误产生概率，验证主要降低错误逃逸概率。
8. 沙箱、备份和回滚主要降低错误造成的损失。
9. 高风险规则不能被综合评分或高模型信任度抵消。
10. 规则、状态、验证和证据应尽可能代码化和结构化。
11. 所有批准和证据必须绑定具体版本。
12. 先建立可靠的最小闭环，再引入复杂公式和自动模型路由。

---

## 总体定位

> 本系统不是单一 Skill，而是一套由 Agent 规则、Skills、角色 Agent、Policy as Code、任务状态机、Verification as Code、Hooks、CI 和可复现证据共同组成的 AI 代码协同控制系统。第一版以可运行、可审计、可升级为目标，后续再逐步优化模型选择、费用和可靠性。
