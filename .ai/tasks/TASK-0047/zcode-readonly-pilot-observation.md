# ZCode 单次只读接入观察

## 范围与事实

2026-09-08，所有者单独授权一次使用现有按量模型的只读试点。授权记录见
`action-paid-zcode-readonly-smoke.json`，本地审计提交为
`0d9c157163d0b5b947a0fae66a04280398184ead`。这不是新的实现任务，也不复用此前发布授权。

- 在已安装的 ZCode 3.11.2.6792 中打开本仓，保留当时的 BigModel 按量 API /
  GLM-5.3-Flash、思考等级与访问模式；未改变账户、模型、权限或全局配置。
- 界面只提交一次限定范围的提示词。可见工具记录是六次原生 Read，文件分别为
  `AGENTS.md`、`docs/operations/low-intervention.md`、`docs/operations/adoption.md`、
  `examples/adoption/zcode-pilot-prompt.md`、`pyproject.toml` 与
  `.github/workflows/ai-quality-gate.yml`，没有扩展到链接文件。
- ZCode 输出了完整中文报告并停止，界面恢复为可发送状态。没有发送后续提示词或重试；
  可见记录没有终端、写入、网络搜索、委派或后台动作，也没有要求追加人工批准。
- 执行前后，主协调方本地 Git 检查均为干净；结束时 HEAD 仍为上述审计提交。
  此处的本地检查由主协调方执行，不是通过 ZCode 界面要求其运行命令。
- 本轮审计通过 CLI validate、134 项契约/Policy/模板/审批回归与独立审查。
  没有重新运行完整业务测试，也没有新的推送、合并、删除或部署。

## 结果及证据限度

窄范围结果：文件可达、主要规则可理解、只读边界在本次可见行为中得到遵守。
报告正确区分了维护模式的 task-free 例外、必须升级的范围、仍需单独授权的外部动作，
以及已经授权的机械步骤无需反复询问；也没有把 Codex 专属模型路由套用到 ZCode。

ZCode 自述在显式读取之前收到工作区 `AGENTS.md` 的注入副本，并称其与磁盘版一致。
这是模型自述，界面可直接佐证的是六份文件的显式读取；没有独立的加载器跟踪，不能
把这次结果写成自动加载机制的完整验证。报告中“内容与注入副本一致”的措辞也不应
扩展为六份文件都自动加载。

报告将未读取的 bootstrap 标记、Git/CI 当前事实及账户级能力标为未验证；未执行任何
质量检查入口。个别入口或产品能力描述仍只是报告转述，不能作为后续命令的只读保证
或独立能力验收。用户级配置原文、原始会话及截图不纳入本记录。

一次用户提示词可能涉及多轮底层模型推理，不等同于一次 provider API 请求。
未读取账单，实际 token 计费、费用、延迟分项及所有后台内部行为均未独立核实。
本次显式付费批准是必需动作授权，不能据此计算日常业务任务的人工介入率。

## 停止边界

本次没有发现必须修改仓库规则才能继续轻量试点的问题，因此不新增配置或实现。
它不证明真实编码效率、缺陷率或完整 AI Flow 引擎接入。下一步应选择一个已有真实
小任务观察，而不是为试点制造代码变更；后续付费调用和外部动作不在本次授权内。

这份文字是可见事实的人工观察记录，不是系统签发的 action 消费 receipt，不改写
TASK-0047 的冻结 subject、实现证据或 MERGED 状态。本轮记录仅在本地保存，未发布。
