## 内容

- 提供轻量接入指南、可按需合并的项目规则及 ZCode 首轮提示词；推广前保留目标项目规则，
  区分流程审核与产品权限弹窗，不默认复制完整审批链。
- 新增标准库只读仓库盘点：Git 根/分支/状态与固定指令、CI、质量配置入口；不运行目标
  Hook、过滤器或代码，不网络、不写目标，不把盘点结果解释为准入或授权。
- 明确完整 AI Flow 的跨仓库初始化/运行目录/风险事实/CI 适配缺口，不宣称即装即用，
  不改变历史任务处置、维护模式、阶段三/四或现有质量门。

## 验证

- 已提交实现 `5a9681e`：Windows 全量 1,740 passed，总覆盖率 88.12%。
- 工具定向 19 passed，单独 branch coverage 91.79%，diff coverage 93.4%。
- 提交后干净克隆安装与工具组合测试 20 passed；根/子目录盘点一致，Git 索引保持不变。
- Ruff、format、mypy、补充工具 strict mypy、whitespace 与独立审查通过。
- 初次预提交 clean-clone 的暂存/HEAD 清单失败如实保留在说明中，没有放宽原测试。

## 交付边界

所有者明确授权本轮推送、创建 PR 和通过检查后的合并。TASK-0047 仅追加两个新的单次
动作审计，不修改其原实现和 evidence，不再次 close。仅在本 PR 精确 head 的 required
ai-quality-gate 成功后正常合并；不管理员绕过、不删除分支、不部署、不修改 ZCode/
外部项目/全局模型配置，不创建递归关闭 PR，也不宣称真实效率改善已经验证。
