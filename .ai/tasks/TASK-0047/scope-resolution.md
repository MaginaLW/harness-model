# 初稿范围问题的技术处置

REV-0078 指出的两个问题均在实现和规格批准前处理。初稿及其上下文已保留在提交
`d890c0f`，没有覆盖审查历史，也没有取得或复用任何人类批准。

- RF-001：移除三个测试/夹具目录 glob 及不必要的 acceptance 文件范围，改为 task.yaml
  中逐个列出的回归、共享夹具与四个可执行示例输入。没有允许整个 tests、examples 或 docs
  目录；不允许改 expected 快照、历史 task 或 mutation 执行器。
- RF-002：撤回“这些必要回归是独立 task-free”的初稿说法。新的分类写前契约会使既有
  测试输入失效，必要夹具与代码需一起通过完整验证。规格明确提出仅此精确范围的所有者
  隔离例外，并在请求中直接说明；尚未获批，Agent 不据此自行豁免 AGENTS 或执行实现。
  无关安全工作仍独立；已完成的效果文档基线在本任务 base 之前，未借用未来验证。

两个 DU 的治理目标、REVIEW/V1 事实、禁止外部动作、质量阈值、Policy 自用恢复、历史
兼容及默认拒绝语义均不降低。范围/规格变化按 CLI escalation/resolution/reclassify/freeze
重新绑定；这只是 Agent 完成技术审查的机械迭代，不能记录为已发生的人类决定。
