# TASK-0028 H1 isolated CI simulation action-use receipt

## Binding

- Action SHA-256:
  `f1d0fec813c2f8f00ede4ba2e9ff7b7c7d0882718f597714a266a5c46ebeb228`
- Action-file byte SHA-256:
  `1bf529c97304e3ba3aa5d6bb8467fffd138a438151af037eac6d86b11d1349bf`
- Transaction: `TASK-0028-H1-CI-001`
- Decision unit: `DU-001`
- Subject: `b7c7fb4a0bab1a2a4b0d9a59edfe237839f3779d`
- H1 code-approval attestation:
  `f35d3094a8385806abff2996691e5224bedb00e2`
- Frozen specification:
  `657bac588589c0fabb9c8d6a108ab9077cd101c9648f55d0d3f5e71299f06877`
- Policy:
  `f4854d7fa05e5bddc21303350476bf47568bfe50f64c9d1f9199c0d744321bbf`
- Classification input:
  `28204d9e485e11ca0948982038798e99f8148512a491550e80f4ebeb1e14a75a`
- Action status: `consumed`
- Transaction target creation began at `2026-08-29T17:18:21Z`.
- Final audit observation was recorded at `2026-08-29T17:24:11Z`.

## Preflight

- The canonical action digest, subject, approval, expiry, specification, Policy,
  classification, implementation review `REV-0056`, H1 final evidence, code approval and
  local read-only Gate were current.
- The source branch, source HEAD and `refs/heads/main` all identified the H1 attestation.
- The two exact OS-temporary direct-child targets were absent before launch.
- The locked project/environment inputs matched the action: Python `3.11.9`, lock-file,
  project metadata, copied-environment metadata, editable-path and sorted environment
  inventory hashes were all current.
- H1 final evidence byte SHA-256 was
  `4fc5729ef5e40f468b8966e35696a84e47b2de05e363d517293ac9e2f9823662`;
  immutable mutation artifact digest was
  `2028bd7cbdeebd26033e3d8dd36728e87362f71f638f2a89087425f73da8530e`.

## Execution

- One CI run directory and one same-branch isolated worktree were created at the exact
  approved OS-temporary direct-child targets.
- The isolated worktree was `main` at the exact H1 attestation and had no tracked changes.
- The existing offline environment was copied once into the isolated worktree. Only the
  copied editable-path file was changed, from the source checkout to the isolated checkout.
  The copied interpreter then resolved `aiflow` from the isolated checkout.
- Exactly six immutable targeted-mutation JSON files were copied to the matching ignored
  replay location; all copied byte hashes matched their source files.
- CI verification was launched exactly once, without an explicit actor, using the isolated
  interpreter and checkout. It failed before creating a plan, running any Policy check or
  writing external evidence with the fail-closed summary
  `V2 local-final mutation evidence is invalid`.
- Normalized failure-summary SHA-256:
  `f3cd0ecf5e93bf77756a2f5b1292d7a5a0581cbafef897f045c93737a37157b8`.
- The CI run directory contained zero files. No external CI evidence existed, so Gate was
  not invoked.
- No mutation recorder, action consumer, mutation runner, new mutation worktree collector,
  network operation, external model or paid call was invoked.

## Zero-write and Git checks

- The complete pre-existing source TASK-0028 file manifest was byte-identical before and
  after CI verification. Stable manifest SHA-256:
  `daa9322f63adaa032f311e7aacf6cabbd273ce4c0d1e4fff798293ff1a6829ae`.
- Source Git status was byte-identical before and after verification. Stable status SHA-256:
  `37825445fa43866f5859e590616228946d63bce40308dbbecffe984a4c67b7f7`.
- All Git refs were byte-identical before and after verification and cleanup. Stable refs
  SHA-256: `5a308a1ef9ac28c67d409b0e7c8210c52af7a444e0b77a71fae8b7d35df1404f`.
- The Git worktree registry returned exactly to its pre-transaction value after approved
  worktree cleanup. Stable registry SHA-256:
  `6a1de282971d26e4dc001a3f09147a5764dd1cbfb6aba80f401ccb5c7f809563`.
- Source HEAD remained `f35d3094a8385806abff2996691e5224bedb00e2`.

## Cleanup

- The exact transaction-created isolated worktree, including its copied environment and
  replay fixture, was removed successfully through the approved Git worktree command.
- The exact CI run-directory target remained empty. Both approved PowerShell
  `Remove-Item -LiteralPath` cleanup launches were rejected by the host before process
  creation, including the non-recursive empty-directory form. No partial deletion occurred.
- No unapproved fallback deletion mechanism was used. One empty OS-temporary direct-child
  directory therefore remains and requires a separately approved cleanup mechanism.

## Result and limitations

- Result: `failed_before_plan`.
- Required checks executed: `0/14`.
- External evidence: `absent`.
- External Gate: `not_run`.
- Source task zero-write: `passed`.
- Source refs unchanged: `passed`.
- Worktree cleanup: `passed`.
- Run-directory cleanup: `blocked_empty_residual`.
- This action was consumed by launch and must never be retried or reused.
- The generic fail-closed mutation replay rejection is preserved; its precise internal cause
  is not yet verified. Any diagnostic remediation, residual cleanup or new isolated CI
  simulation requires fresh governance and a separately approved action.
- H2 state projection, H2 V2, targeted mutation, push, merge, deploy and task close were not
  performed or authorized. Chapter 13.3 remains pending.
