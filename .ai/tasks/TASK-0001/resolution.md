# BLOCK resolution

The user confirmed `OPT-01；批准；确认`: do not delete any scenario file and replace the original
request with a dry-run inventory only.

The pre-change scenario hashes are stored in the external PILOT-BLOCK artifact directory. The
task now declares a reversible documentation-only action, automatic verification, a dry run, and
an explicit single-file impact scope.
