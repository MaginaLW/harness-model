# TASK-0046 设计核查与批准问题

## 当前事实

CLI 已分类并冻结本任务：REVIEW / V1，WAITING_FOR_SPEC_REVIEW；`status` 唯一缺项为
`spec_approval`。尚未执行 begin、修改源码、运行本任务完整验证或取得 spec/code 批准。
旧候选与旧 TASK-0042 的失败证据均未搬入。

## 冻结规格的时态澄清

冻结规格中“V1 已运行整套 pytest”应理解并澄清为“现行 V1 检查计划包含整套 pytest，
本任务实现后将运行它”，不是 TASK-0046 已完成验证的声明。本补记只纠正表达的时态，
不改变冻结的验收条件、范围、风险、分流、验证等级或权限；原规格与记录保留。

独立文档阶段 `47b7de4` 已完成 32 项统计/入口/技能/验收追踪定向测试与 whitespace
检查；这不能替代尚未开始的 ASK 修复验证。

## 独立技术审查

独立审查已核对当前冻结规格、任务事实及源码契约，未发现阻断性缺项：

- matched_rules 来自既有已校验分类条目；不把 helper 的兼容回退宣称为契约验证。
- 同/更低档重新分类丢失 ASK 才触发新增降级检测；更高档与 BLOCK 保持既有升级处理。
- 实际 CLI 生命周期、单元绑定、REVIEW 和 BLOCK 保留、坏分类拒绝均有明确验收条件。
- 四组测试只验证本次治理行为；独立效果基线已先按 task-free 方式提交。
- V1 完整质量检查与额外技术审查足以作为当前计划；没有自行新增 V2 专属协议或放宽门槛。

上述是技术审查结论，不替代项目所有者的设计/风险批准。

## 唯一待决定项

是否批准该最小修复规格并允许实现：同一单元 REVIEW+ASK 共同命中时先完成 ASK，
再进入原 REVIEW 审核；复用 matched_rules 并保留 BLOCK、权限、批准及质量门？

需要接受的实际影响是：此前被静默遗漏、但本来就必要的方向选择会重新阻止自动推进。
纯 REVIEW 不会因此新增 ASK，ASK 回答也不能替代 spec/code 审核。不在本次实现中处理
风险字段缺失、自由文本动作、通用动作授权、阶段三或历史 TASK-0028。

批准后 Agent 将连续完成允许的实现、回归、完整验证、技术审查与阶段提交；后续所需
条件仍以 CLI status/Gate 为准，不把本设计批准当作未来代码或外部动作授权。

## 2026-09-07 规格批准与实现启动补记

项目所有者明确回复“批准”，覆盖上述冻结规格与实现启动，不包含代码接受或外部动作。
CLI 首次记录批准时指出缺少结构化设计审查；Agent 将独立审查结论补录为 REV-0076 后，
同一份用户授权成功记录，随后 begin 进入 IMPLEMENTING。该机械补录没有再次请求人类
决定，也没有改写规格、分类事实或历史记录。

实现复用旧候选的最小行为修复，但纠正了 helper 对非法输入的说明：它读取已校验分类，
不承担持久化契约校验。新增回归还覆盖非法分类零写拒绝及更高路由升级不受误阻断。

## 2026-09-07 实现与完整验证补记

实现提交为 `2918926`，生命周期与单元绑定测试提交为 `4133230`；sync 将 subject 绑定到
`41332307250675d6e5802d4a3cf2b8e086613184`，未更改已批准的规格或分类输入。
完整 V1 运行 `run-20260907T141446453478Z` 的 10 项必需检查全部通过：1,092 项单元测试、
1,657 项全量回归、1,657 项覆盖率轮次；含分支的总覆盖率 87.98%，独立 85% 门槛检查通过，
19 行可执行差异覆盖率 100%。Ruff、format、mypy、whitespace 及范围/契约均通过。

独立交叉审查发现的测试问题已修复：Gate 夹具改用真实一致的两单元分类和新鲜度绑定的
错单元/正确单元事件，明确排除 classification stale；整合时移除未实现的测试辅助参数。
修正后两份 answer/Gate 测试共 47 项通过，另两份 routing/classify 共 41 项通过，再完成
上述完整 V1。没有将旧任务的失败证据或批准迁入。

验证后状态为 WAITING_FOR_FINAL_REVIEW，status 的唯一缺项是 code_approval。批准前的
Gate 返回 GATE_STATE_INVALID 与 GATE_CODE_APPROVAL_STALE，是等待代码批准及其状态
推进的预期结果；没有分类、规格或验证证据失效原因。其通用 recovery_argv 中的 verify
不是本次证据失效的证明，不为消除通用提示重复验证或重复请求规格批准。
代码接受仍须项目所有者决定，外部动作仍未授权。完整审核材料见 review-package.md。

## 2026-09-07 最终技术审查补记

独立实施技术审查已通过 CLI 记录为 REV-0077，推荐 APPROVE，findings 为空。
实施上下文绑定 `7257dc0c59711a1c444ed6308a6ac2ab6ffdca7902fdd0212fb8bcfcaeea1013`。
核查时区分了两种证据摘要：CLI 使用规范化 JSON artifact digest
`24b6edebea8c1ed609e51347a9ac38a2cd8a46b55353330fcef6142e292b3f92`，并非格式化 JSON 文件
字节的 SHA-256。重新运行 review context 与独立复算一致，没有改写证据来迁就另一口径。
审查记录仅表示技术建议，不代替当前仍待取得的人类 code 批准。

## 2026-09-07 代码批准与最终 Gate 补记

项目所有者明确批准实现审核包。CLI 已在 14:39:13 UTC 记录 code 批准，绑定已验证的
subject `41332307250675d6e5802d4a3cf2b8e086613184`、原规格和当前证据，并进入
APPROVED_FOR_MERGE。随后实际 Gate 通过，reason_codes 与 recovery_argv 均为空；
分类、批准和证据保持有效，无需重复规格批准或完整验证。

本次仅追加批准及本任务记录，没有修改实现或既有证据。status 所示 external_merge
是尚未发生的外部交付，不是新的内部代码审批缺项；不得据此直接记录 MERGED。
代码批准不授权推送、创建远端 PR、合并或部署，这些动作仍未执行。
