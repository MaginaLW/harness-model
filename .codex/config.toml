# Model and reasoning choices belong to the user and current runtime.
# Keep project model overrides unset; see docs/operations/model-selection.md.

[agents]
enabled = true
max_concurrent_threads_per_session = 2
